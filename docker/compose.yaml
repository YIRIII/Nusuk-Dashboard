services:
  api:
    build:
      context: ..
      dockerfile: docker/Dockerfile
    image: hadaq-api:local
    environment:
      NODE_ENV: production
      PORT: 3001
      LOG_LEVEL: info
    ports:
      - "3001:3001"
    restart: unless-stopped
