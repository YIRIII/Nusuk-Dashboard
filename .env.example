# Copy to `.env` and fill in. `.env` is gitignored; never commit real keys.

# Server
PORT=3001
LOG_LEVEL=info

# Chromium (leave unset to auto-detect system Chromium)
# CHROME_BIN=/usr/bin/chromium

# Supabase
SUPABASE_URL=https://dwbpykdltqsehqeextjq.supabase.co
SUPABASE_SERVICE_ROLE_KEY=
SUPABASE_CAPTURES_BUCKET=captures

# Browser pool
BROWSER_POOL_MIN=1
BROWSER_POOL_MAX=2
